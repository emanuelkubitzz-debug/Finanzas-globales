# Mercados Globales - Backend Demo

Backend Node.js + Express + SQLite para pruebas. No procesa dinero real.

En Render: Root Directory `backend` si esta carpeta está dentro del repositorio; Build Command `npm install`; Start Command `npm start`. Añade `JWT_SECRET` como Environment Variable.
